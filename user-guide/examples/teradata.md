Example Teradata table DDL

## Varchar Table
```sql
CREATE MULTISET TABLE listener.test_varchar ,NO FALLBACK ,
     NO BEFORE JOURNAL,
     NO AFTER JOURNAL,
     CHECKSUM = DEFAULT,
     DEFAULT MERGEBLOCKRATIO
     (
      uuid VARCHAR(55) CHARACTER SET LATIN CASESPECIFIC,
      source_id VARCHAR(55) CHARACTER SET LATIN CASESPECIFIC,
      time_stamp TIMESTAMP(6) WITH TIME ZONE,
      data VARCHAR(64000) CHARACTER SET LATIN NOT CASESPECIFIC)
PRIMARY INDEX ( uuid );
```


## Clob Table
```sql
CREATE MULTISET TABLE listener.test_clob ,NO FALLBACK ,
     NO BEFORE JOURNAL,
     NO AFTER JOURNAL,
     CHECKSUM = DEFAULT,
     DEFAULT MERGEBLOCKRATIO
     (
      uuid VARCHAR(55) CHARACTER SET LATIN CASESPECIFIC,
      source_id VARCHAR(55) CHARACTER SET LATIN CASESPECIFIC,
      time_stamp TIMESTAMP(6) WITH TIME ZONE,
      data CLOB(2097088000) CHARACTER SET LATIN)
PRIMARY INDEX ( uuid );
```

## Json Table
```sql
CREATE MULTISET TABLE listener.test_json ,NO FALLBACK ,
     NO BEFORE JOURNAL,
     NO AFTER JOURNAL,
     CHECKSUM = DEFAULT,
     DEFAULT MERGEBLOCKRATIO
     (
      uuid VARCHAR(55) CHARACTER SET LATIN CASESPECIFIC,
      source_id VARCHAR(55) CHARACTER SET LATIN CASESPECIFIC,
      time_stamp TIMESTAMP(6) WITH TIME ZONE,
      data JSON(16776192) CHARACTER SET LATIN)
PRIMARY INDEX ( uuid );
```
